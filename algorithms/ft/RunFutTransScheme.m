function FutTransConsensus = RunFutTransScheme(E, Params)
% Function performs FutTrans consensus clustering scheme.
% Params is supposed to be empty and present here just to be in account with format

%% Run

FutTransConsensus=ft(E);

